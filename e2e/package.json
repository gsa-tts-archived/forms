{
  "name": "@gsa-tts/forms-e2e",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "auth": "pnpm --filter=@gsa-tts/forms-cli cli secrets generate-test-db-session -p ../../packages/server/src/main.db -o ../../e2e/.env",
    "dev": "tsc -w",
    "test:storybook": "test-storybook",
    "pretest:dev": "pnpm auth",
    "pretest": "pnpm auth",
    "test:dev": "pnpm playwright test --ui-port=8080 --ui-host=0.0.0.0",
    "test": "pnpm playwright test"
  },
  "devDependencies": {
    "@storybook/test-runner": "^0.19.1",
    "dotenv": "^16.4.7",
    "path-to-regexp": "^8.2.0"
  },
  "dependencies": {
    "@gsa-tts/forms-common": "workspace:*"
  }
}
